{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-dos.dev/schemas/qc-decision.schema.json",
  "title": "QcDecision",
  "type": "object",
  "additionalProperties": false,
  "required": ["task_id", "decision_id", "decision", "reason_codes", "prior_artifacts", "decided_by", "decided_at"],
  "properties": {
    "task_id": {"type": "string", "pattern": "^TASK-[0-9]{4}-[0-9]{6}$"},
    "decision_id": {"type": "string", "pattern": "^QC-[0-9]{6}-[0-9]{2}$"},
    "decision": {"type": "string", "enum": ["pass", "fail"]},
    "reason_codes": {"type": "array", "minItems": 1, "items": {"type": "string", "minLength": 2, "maxLength": 100}, "uniqueItems": true},
    "prior_artifacts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["review_report_id", "security_report_id", "test_run_id"],
      "properties": {
        "review_report_id": {"type": "string", "pattern": "^REV-[0-9]{6}-[0-9]{2}$"},
        "security_report_id": {"type": "string", "pattern": "^SEC-[0-9]{6}-[0-9]{2}$"},
        "test_run_id": {"type": "string", "pattern": "^TEST-[0-9]{6}-[0-9]{2}$"}
      }
    },
    "decided_by": {"type": "string", "minLength": 1, "maxLength": 100},
    "decided_at": {"type": "string", "format": "date-time"},
    "notes": {"type": "string", "maxLength": 1000}
  }
}